import { AlertColor, SnackbarCloseReason } from "@mui/material";
import { SyntheticEvent } from "react";
import { SnackbarProps } from "../snackbarAPI.types";

export type SnackbarAPIProps = {
	snackbarProps: SnackbarProps;
};
